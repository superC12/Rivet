# Copy to .env. Rivet never returns these values through its API.
OPENROUTER_API_KEY=
N8N_ACTION_KEY=

# Optional path overrides
# RIVET_CONFIG_DIR=/path/to/config
# RIVET_DATA_DIR=/path/to/data
# RIVET_DATABASE_PATH=/path/to/rivet.db
